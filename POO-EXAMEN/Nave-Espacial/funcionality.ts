import {event} from './event';
import {planet} from './planet';

export function explore(direction: 'north' | 'south' | 'east' | 'west'): void {
    //  Implementamos la funcion explorar.
}

export function gatherResources<T>(planet: planet, amount: number) : T[] {
    // Implementamos la funcion recolectar.
    return [];
}

export function handleevent(event: event):void{
    // Agregamos la funcion  manejar eventos a nuestro personaje
}

